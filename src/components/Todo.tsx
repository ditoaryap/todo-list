// import React, { useState } from "react";
// import Form from "./Form";

// const Todo = () => {
//   const [list, setList] = useState<string[]>([]);

//   const addTask = (task: string) => {
//     setList([...list, task]);
//   };

//   return (
//     <div>
//       <Form addTask={addTask} />
//       {/* Menyampaikan fungsi addTask sebagai prop */}
//       <ul>
//         {list.map((task, index) => (
//           <li key={index}>{task}</li>
//         ))}
//       </ul>
//     </div>
//   );
// };

// export default Todo;
